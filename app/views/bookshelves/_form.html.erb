<%= form_with(model: bookshelf, local: true) do |form| %>
  <% if bookshelf.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(bookshelf.errors.count, "error") %> prohibited this book from being saved:</h2>

      <ul>
      <% bookshelf.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
